import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    map_container: {
        flex: 1
      },
    map: {
      flex: 1
    },
    overlay: {
      position: 'absolute',
      bottom:0
    }
  })
